{
  "name": "@meal-diary/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "nest build",
    "lint": "tsc --project tsconfig.json --noEmit --incremental false",
    "test": "tsc --project tsconfig.json --outDir dist-test && node --test dist-test/*.spec.js",
    "start": "nest start",
    "start:dev": "nest start --watch"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.0",
    "@nestjs/core": "^10.4.0",
    "@nestjs/platform-express": "^10.4.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.4.0",
    "@nestjs/schematics": "^10.2.3",
    "@nestjs/testing": "^10.4.0",
    "@types/node": "^20.16.5",
    "typescript": "^5.6.2"
  }
}
