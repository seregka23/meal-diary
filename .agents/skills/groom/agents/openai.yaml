interface:
  display_name: "Plan and groom AI-first work"
  short_description: "Plan large items or prepare approval-ready briefs"
  default_prompt: "Use $groom to plan this large item into regular items or prepare an approval-ready brief."
policy:
  allow_implicit_invocation: false
